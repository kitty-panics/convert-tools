#!/usr/bin/env python3

#
# Character to unicode code point
#
# 注：
# 需要系统已装 python3 包
#

# 导模块
import sys

# 定义变量
GET_INPUT = sys.argv[1]

# 转换
print(hex(ord(GET_INPUT)))
#print(hex(ord(GET_INPUT))[2:])
